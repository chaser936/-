hour,minute = map(int,input().split())
add = int(input())
mod = 0

minute += add

if(minute > 59):
    hour += (minute // 60)
    minute %= 60
    if(hour > 23):
        hour -= 24
        
print(hour, minute)
    
